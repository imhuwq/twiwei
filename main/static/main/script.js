$(document).ready(function () {
    // var msg_imgs = $('.cls-message-img');
    // msg_imgs.each(function () {
    //     var full_width = $(this).width();
    //     var wrapper = $(this).children('.cls-img-wrapper');
    //     if (wrapper.length == 2) {
    //         wrapper.each(function () {
    //             $(this).width(full_width * 0.48);
    //             $(this).height($(this).width());
    //
    //         })
    //     }
    //     else if (wrapper.length >= 3) {
    //         wrapper.each(function (index) {
    //             if (index == 0) {
    //                 $(this).width(full_width * 0.64);
    //                 $(this).height($(this).width());
    //             }
    //             else {
    //                 $(this).width(full_width * 0.32);
    //                 $(this).height($(this).width());
    //
    //             }
    //         })
    //     }
    // })
});